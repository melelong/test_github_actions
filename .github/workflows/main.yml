name: Main

on:
  push:
    branches: ['main', 'test', 'dev']

jobs:
  build:
     runs-on: ubuntu-latest
     steps:
      - uses: actions/checkout@v4
      - name: Print Main
        run: |
          echo "Main"
